import styled from "styled-components";

const Navigation = () => {
  return <Wrap>Philosophy / Collection / Space / Contact</Wrap>;
};

export default Navigation;

const Wrap = styled.nav`
  // border: solid 1px blue;
`;
